#version 330 core

uniform samplerCube envirmentTexture;
uniform vec3 eye_w;

in vec4 v_normal;
in vec4 v_pos;

out vec4 color;

void main() {
    vec3 view_dir = normalize(eye_w - v_pos.xyz);
    color = texture(envirmentTexture, reflect(view_dir, v_normal.xyz));
}